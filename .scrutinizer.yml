build:
  nodes:
    coverage:
      tests:
        environment:
          php:
            version: 7.4
        override:
          - command: php ./vendor/bin/phpunit --coverage-clover tests/coverage/coverage.clover
            coverage:
              file: tests/coverage/coverage.clover
              format: clover
filter:
  excluded_paths: [vendor/*, tests/*]