build:
  nodes:
    my-tests:
      environment:
        php:
          version: 7.4
      tests:
        override:
          - command: ./vendor/bin/phpunit --coverage-clover tests/coverage/coverage.clover
            coverage:
              file: tests/coverage/coverage.clover
              format: php-clover
filter:
  excluded_paths: [vendor/*, tests/*]